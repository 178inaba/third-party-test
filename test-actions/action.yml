name: Test Actions

description: This is test.

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Setup Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.22'
