name: Test Actions

description: This is test.

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v7
    - name: Setup Go
      uses: actions/setup-go@v7
      with:
        go-version: '1.22'
