name: Test
description: Run tests

runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v5
    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
    - name: Test
      shell: bash
      run: make -s test
